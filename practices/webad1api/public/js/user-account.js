console.log(currentPath, window.location.hash);

const userAccContent = document.getElementById("user-account-content");

const toLoginBtn = document.getElementById("userLoginBtn");
const toRegisterBtn = document.getElementById("userRegisterBtn");

const loadUserLogin = () => {};

toRegisterBtn.addEventListener("click", (e) => {
	console.log();
});
